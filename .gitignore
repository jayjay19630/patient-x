.claude/

# Rust build artifacts
target/
**/*.rs.bk
*.pdb

# Cargo lock file (workspace generates its own)
# Cargo.lock is committed at workspace root for reproducible builds
/parachains/identity-consent-chain/Cargo.lock